import { Class } from '@loopback/repository/dist/common-types';
import { Model } from '@loopback/repository/dist/model';
import { ObjectType } from '@loopback/repository/dist/types/object';
/**
 * Model type
 */
export declare class ModelType<T extends Model> extends ObjectType<T> {
    modelClass: Class<T>;
    readonly name: string;
    constructor(modelClass: Class<T>);
    serialize(value: T | null | undefined): Object | null | undefined;
}
